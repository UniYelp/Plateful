#? Eval
EVAL_RUN_DUMMY_ONLY=true #? Set to `true` to run only the dummy eval case
EVAL_RUN_DUMMY=true  #? Set to `true` to run the eval with dummy outputs
EVAL_RUN_BAD_DUMMY=true #? Set to `true` to run the eval with bad dummy outputs
EVAL_RUN_LIVE_MODEL=false #? Set to `true` to run the eval with the actual models

#? AI

##? Google
GOOGLE_GENERATIVE_AI_API_KEY="" #? Copy from file:///./../../apps/api/.env