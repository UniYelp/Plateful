#? Eval
EVAL_DUMMY_ONLY=true #? Set to `false` to run the eval with the actual models
EVAL_BAD_DUMMY_OUTPUT=false #? Set to `true` to run the eval with bad dummy outputs

#? AI

##? Google
GOOGLE_GENERATIVE_AI_API_KEY="" #? Copy from file:///./../../apps/api/.env