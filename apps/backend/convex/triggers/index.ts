import { Triggers } from "convex-helpers/server/triggers";
import type { DataModel } from "../_generated/dataModel";

/**
 * {@link https://stack.convex.dev/triggers}
 */
export const triggers = new Triggers<DataModel>();
